---
# docs: https://packit.dev/docs/configuration/
upstream_package_name: rpmlint
specfile_path: .packit/rpmlint.spec

current_version_command: [".packit/generate-version.sh"]
#create_tarball_command: [".packit/generate-tarball.sh"]

jobs:
- job: copr_build
  metadata:
    targets:
    - fedora-rawhide-x86_64
    - mageia-cauldron-x86_64
    - opensuse-tumbleweed-x86_64
  trigger: pull_request
